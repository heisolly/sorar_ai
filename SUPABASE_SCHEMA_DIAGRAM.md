# Supabase Database Schema Diagram

## 📊 Database Structure Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        SORAR AI DATABASE                         │
│                     PostgreSQL 17.6.1                            │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      AUTHENTICATION                               │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ auth.users (Supabase managed)
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│user_profiles  │    │user_settings  │    │user_progress  │
├───────────────┤    ├───────────────┤    ├───────────────┤
│• id (PK)      │    │• id (PK)      │    │• id (PK)      │
│• user_id (FK) │    │• user_id (FK) │    │• user_id (FK) │
│• name         │    │• theme        │    │• metric_type  │
│• gender       │    │• notifications│    │• metric_value │
│• age          │    │• language     │    │• metadata     │
│• main_goals[] │    │• timezone     │    │• created_at   │
│• challenges[] │    │• created_at   │    │• updated_at   │
│• anxiety_freq │    │• updated_at   │    └───────────────┘
│• confidence   │    └───────────────┘
│• comm_style   │
│• created_at   │
│• updated_at   │
└───────────────┘
        │
        │
        ▼
┌───────────────┐    ┌───────────────┐
│user_goals     │    │journal_entries│
├───────────────┤    ├───────────────┤
│• id (PK)      │    │• id (PK)      │
│• user_id (FK) │    │• user_id (FK) │
│• title        │    │• title        │
│• description  │    │• content      │
│• category     │    │• mood         │
│• target_date  │    │• tags[]       │
│• status       │    │• is_private   │
│• progress_%   │    │• created_at   │
│• metadata     │    │• updated_at   │
│• created_at   │    └───────────────┘
│• updated_at   │
│• completed_at │
└───────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      CHAT SYSTEM                                  │
└──────────────────────────────────────────────────────────────────┘

┌───────────────┐
│chat_sessions  │
├───────────────┤
│• id (PK)      │
│• user_id (FK) │
│• scenario_id  │
│• status       │
│• score        │
│• created_at   │
│• updated_at   │
└───────┬───────┘
        │
        │ 1:N
        │
        ▼
┌───────────────┐
│chat_messages  │
├───────────────┤
│• id (PK)      │
│• session_id(FK)│
│• sender       │
│• content      │
│• audio_url    │
│• feedback     │
│• created_at   │
└───────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      ANALYTICS                                    │
└──────────────────────────────────────────────────────────────────┘

┌───────────────┐    ┌───────────────┐
│waitlist       │    │site_visits    │
├───────────────┤    ├───────────────┤
│• id (PK)      │    │• id (PK)      │
│• email        │    │• user_agent   │
│• created_at   │    │• referrer     │
└───────────────┘    │• path         │
                     │• timestamp    │
                     └───────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      DATABASE VIEWS                               │
└──────────────────────────────────────────────────────────────────┘

┌─────────────────────────────┐
│chat_sessions_with_counts    │
├─────────────────────────────┤
│Aggregates:                  │
│• Session details            │
│• Message count              │
│• Last message timestamp     │
└─────────────────────────────┘

┌─────────────────────────────┐
│user_statistics              │
├─────────────────────────────┤
│Aggregates:                  │
│• Total chat sessions        │
│• Total messages             │
│• Completed goals            │
│• Active goals               │
│• Journal entries count      │
│• Last chat session          │
└─────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      STORAGE BUCKETS                              │
└──────────────────────────────────────────────────────────────────┘

┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│app_storage      │  │avatars          │  │chat_attachments │
├─────────────────┤  ├─────────────────┤  ├─────────────────┤
│Public: ✅       │  │Public: ✅       │  │Public: ❌       │
│Limit: None      │  │Limit: 5 MB      │  │Limit: 10 MB     │
│Types: All       │  │Types: Images    │  │Types: Images,   │
│                 │  │                 │  │       PDF, Text │
└─────────────────┘  └─────────────────┘  └─────────────────┘

┌─────────────────┐
│voice_recordings │
├─────────────────┤
│Public: ❌       │
│Limit: 50 MB     │
│Types: Audio     │
│                 │
└─────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      DATABASE FUNCTIONS                           │
└──────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│handle_updated_at()                                               │
│• Trigger function to auto-update updated_at timestamps          │
│• Applied to all tables with updated_at column                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│handle_new_user()                                                 │
│• Trigger on auth.users INSERT                                   │
│• Auto-creates user_profiles and user_settings entries           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│get_user_full_profile(user_uuid)                                 │
│• Returns complete user profile with settings and progress       │
│• Includes active goals                                          │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      REALTIME SUBSCRIPTIONS                       │
└──────────────────────────────────────────────────────────────────┘

📡 Enabled Tables:
• chat_messages      → Live chat updates
• chat_sessions      → Session status changes
• user_progress      → Progress updates
• user_goals         → Goal changes
• user_profiles      → Profile updates

┌──────────────────────────────────────────────────────────────────┐
│                      SECURITY (RLS)                               │
└──────────────────────────────────────────────────────────────────┘

🔒 All 11 tables have Row Level Security (RLS) enabled

Policy Pattern:
┌─────────────────────────────────────────────────────────────────┐
│ Users can only access their own data via:                       │
│ • WHERE user_id = auth.uid()                                    │
│ • Storage: {user_id}/filename structure                         │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                      INDEXES                                      │
└──────────────────────────────────────────────────────────────────┘

Performance Indexes Created:
• idx_user_settings_user_id
• idx_user_progress_user_id
• idx_user_progress_metric_type
• idx_user_goals_user_id
• idx_user_goals_status
• idx_journal_entries_user_id
• idx_journal_entries_created_at

┌──────────────────────────────────────────────────────────────────┐
│                      DATA FLOW                                    │
└──────────────────────────────────────────────────────────────────┘

1. USER SIGNUP
   auth.users → handle_new_user() → user_profiles + user_settings

2. ONBOARDING
   user_profiles ← Update with onboarding data
   user_progress ← Initialize metrics
   user_goals ← Create first goal

3. CHAT SESSION
   chat_sessions ← Create session
   chat_messages ← Add messages (realtime)
   user_progress ← Increment sessions_completed

4. GOAL TRACKING
   user_goals ← Create/Update goals
   user_progress ← Update metrics

5. JOURNALING
   journal_entries ← Create entries
   user_statistics ← Auto-aggregates stats

┌──────────────────────────────────────────────────────────────────┐
│                      LEGEND                                       │
└──────────────────────────────────────────────────────────────────┘

PK  = Primary Key
FK  = Foreign Key
[]  = Array type
✅  = Enabled/True
❌  = Disabled/False
→   = Relationship/Flow
│   = Connection
▼   = Points to
```

## 🔑 Key Relationships

1. **auth.users → user_profiles** (1:1)
   - Auto-created on signup via trigger

2. **auth.users → user_settings** (1:1)
   - Auto-created on signup via trigger

3. **auth.users → user_progress** (1:N)
   - Multiple metrics per user

4. **auth.users → user_goals** (1:N)
   - Multiple goals per user

5. **auth.users → journal_entries** (1:N)
   - Multiple journal entries per user

6. **auth.users → chat_sessions** (1:N)
   - Multiple chat sessions per user

7. **chat_sessions → chat_messages** (1:N)
   - Multiple messages per session

## 📈 Data Size Estimates

Based on typical usage:

| Table           | Avg Row Size | Growth Rate          |
| --------------- | ------------ | -------------------- |
| user_profiles   | 1 KB         | 1 per user           |
| user_settings   | 500 B        | 1 per user           |
| user_progress   | 300 B        | 5-10 per user        |
| user_goals      | 500 B        | 3-5 per user         |
| journal_entries | 2 KB         | 10-20 per user/month |
| chat_sessions   | 300 B        | 5-10 per user/month  |
| chat_messages   | 500 B        | 50-100 per session   |

## 🎯 Query Patterns

### Most Common Queries

1. **Get User Dashboard Data**

   ```sql
   SELECT * FROM get_user_full_profile(auth.uid());
   ```

2. **Get User Statistics**

   ```sql
   SELECT * FROM user_statistics WHERE user_id = auth.uid();
   ```

3. **Get Recent Chat Sessions**

   ```sql
   SELECT * FROM chat_sessions_with_counts
   WHERE user_id = auth.uid()
   ORDER BY created_at DESC
   LIMIT 10;
   ```

4. **Get Active Goals**
   ```sql
   SELECT * FROM user_goals
   WHERE user_id = auth.uid() AND status = 'active';
   ```

## 🔄 Automatic Behaviors

1. **On User Signup**
   - ✅ user_profiles created
   - ✅ user_settings created with defaults

2. **On Row Update**
   - ✅ updated_at automatically set to now()

3. **On Goal Completion**
   - ✅ completed_at set
   - ✅ status set to 'completed'
   - ✅ progress_percentage set to 100

## 🛡️ Security Model

All tables follow this security pattern:

```sql
-- Users can only read their own data
CREATE POLICY "view_own" ON table_name
  FOR SELECT USING (auth.uid() = user_id);

-- Users can only insert their own data
CREATE POLICY "insert_own" ON table_name
  FOR INSERT WITH CHECK (auth.uid() = user_id);

-- Users can only update their own data
CREATE POLICY "update_own" ON table_name
  FOR UPDATE USING (auth.uid() = user_id);

-- Users can only delete their own data
CREATE POLICY "delete_own" ON table_name
  FOR DELETE USING (auth.uid() = user_id);
```

## 📊 Storage Organization

```
Storage Root
├── app_storage/
│   └── [any files]
├── avatars/
│   └── {user_id}/
│       └── avatar.jpg
├── chat_attachments/
│   └── {user_id}/
│       ├── attachment1.pdf
│       └── image1.jpg
└── voice_recordings/
    └── {user_id}/
        ├── recording1.mp3
        └── recording2.wav
```

---

This diagram shows the complete structure of your Supabase database for the Sorar AI application.
